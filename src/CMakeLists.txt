include_directories("axles" "watcher" "gui" "man" "swapper" "config")

add_subdirectory(axles)
add_subdirectory(watcher)
if(COMPILE_GUI)
    add_subdirectory(gui)
endif()
add_subdirectory(man)
add_subdirectory(swapper)
add_subdirectory(config)